# Benchmarks begin in Phase 1 (book-update p99 latency, SPSC throughput).
# Placeholder so the ASMM_BUILD_BENCH option has a valid subdirectory.
message(STATUS "bench/: no benchmarks yet (added in Phase 1)")

# Build output
build/
build-*/
cmake-build-*/
_deps/

# Secrets — NEVER commit (golden rule #1). Use .env.example as the template.
.env

# Runtime artifacts
logs/
*.bin

# Recorded fixture: commit only the compressed tarball (CMake extracts it).
tests/data/depth_btcusdt.jsonl
tests/data/depth_btcusdt.snapshot.json
tests/data/depth_btcusdt.meta.json

# Python (analysis/)
__pycache__/
*.pyc
.venv/
venv/

# OS / editor junk
.DS_Store
*.swp
.idea/
.vscode/
compile_commands.json
